<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Avis Clients Trustpilot</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Arial, sans-serif;
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .reviews-container {
            background: white;
            padding: 60px 40px;
            border-radius: 20px;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
            text-align: center;
            max-width: 800px;
            width: 100%;
            position: relative;
            z-index: 1;
        }

        .main-title {
            font-size: 3.5rem;
            font-weight: 700;
            color: #2c3e50;
            margin-bottom: 40px;
            line-height: 1.2;
            letter-spacing: -0.02em;
        }

        .rating-section {
            margin-bottom: 30px;
        }

        .rating-label {
            font-size: 1.8rem;
            color: #34495e;
            margin-right: 15px;
            font-weight: 500;
        }

        .rating-value {
            font-size: 2.2rem;
            font-weight: 700;
            color: #2c3e50;
            margin-right: 20px;
        }

        .stars-container {
            display: inline-flex;
            gap: 8px;
            margin-left: 10px;
        }

        .star {
            width: 45px;
            height: 45px;
            background-color: #00b67a;
            position: relative;
            transform: rotate(0deg);
            border-radius: 3px;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: transform 0.3s ease;
        }

        .star:hover {
            transform: scale(1.1);
        }

        .star::before {
            content: "★";
            color: white;
            font-size: 28px;
            font-weight: bold;
        }

        .reviews-info {
            margin-top: 25px;
            font-size: 1.4rem;
            color: #7f8c8d;
        }

        .reviews-count {
            font-weight: 700;
            color: #2c3e50;
            text-decoration: underline;
            font-size: 1.5rem;
        }

        .trustpilot-logo {
            display: inline-flex;
            align-items: center;
            margin-left: 15px;
            gap: 8px;
        }

        .trustpilot-star {
            width: 25px;
            height: 25px;
            background-color: #00b67a;
            border-radius: 2px;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .trustpilot-star::before {
            content: "★";
            color: white;
            font-size: 16px;
        }

        .trustpilot-text {
            font-size: 1.3rem;
            font-weight: 600;
            color: #2c3e50;
            margin-left: 5px;
        }

        /* Animations */
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .reviews-container {
            animation: fadeInUp 0.8s ease-out;
        }

        .star {
            animation: fadeInUp 0.6s ease-out;
        }

        .star:nth-child(1) { animation-delay: 0.1s; }
        .star:nth-child(2) { animation-delay: 0.2s; }
        .star:nth-child(3) { animation-delay: 0.3s; }
        .star:nth-child(4) { animation-delay: 0.4s; }
        .star:nth-child(5) { animation-delay: 0.5s; }

        /* Responsive */
        @media (max-width: 768px) {
            .reviews-container {
                padding: 40px 20px;
            }
            
            .main-title {
                font-size: 2.5rem;
            }
            
            .rating-label {
                font-size: 1.4rem;
            }
            
            .rating-value {
                font-size: 1.8rem;
            }
            
            .star {
                width: 35px;
                height: 35px;
            }
            
            .star::before {
                font-size: 22px;
            }
            
            .reviews-info {
                font-size: 1.2rem;
            }
        }

        @media (max-width: 480px) {
            .main-title {
                font-size: 2rem;
                margin-bottom: 30px;
            }
            
            .stars-container {
                gap: 5px;
            }
            
            .star {
                width: 30px;
                height: 30px;
            }
            
            .star::before {
                font-size: 18px;
            }
        }
    </style>
</head>
<body>
    <div class="reviews-container">
        <h1 class="main-title">Nos clients en parlent le mieux</h1>
        
        <div class="rating-section">
            <span class="rating-label">Excellent</span>
            <span class="rating-value">4,7 / 5</span>
            
            <div class="stars-container">
                <div class="star"></div>
                <div class="star"></div>
                <div class="star"></div>
                <div class="star"></div>
                <div class="star"></div>
            </div>
        </div>
        
        <div class="reviews-info">
            basé sur +<span class="reviews-count">1 067</span> avis
            
            <div class="trustpilot-logo">
                <div class="trustpilot-star"></div>
                <span class="trustpilot-text">Trustpilot</span>
            </div>
        </div>
    </div>
</body>
</html>