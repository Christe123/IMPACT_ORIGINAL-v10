<div class="reviews-container">
  <h1 class="main-title">{{ section.settings.title | default: 'Nos clients en parlent le mieux' }}</h1>
  
  <div class="rating-section">
    <span class="rating-label">{{ section.settings.rating_label | default: 'Excellent' }}</span>
    <span class="rating-value">{{ section.settings.rating | default: '4,7' }} / 5</span>
    
    <div class="stars-container">
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
      <div class="star"></div>
    </div>
  </div>
  
  <div class="reviews-info">
    basé sur +<span class="reviews-count">{{ section.settings.review_count | default: '1 067' }}</span> avis
    
    <div class="trustpilot-logo">
      <div class="trustpilot-star"></div>
      <span class="trustpilot-text">Trustpilot</span>
    </div>
  </div>
</div>

<style>
/* Copiez tout le CSS de l'artifact ici */
.reviews-container { /* ... */ }
/* etc. */
</style>

{% schema %}
{
  "name": "Avis Trustpilot",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Titre",
      "default": "Nos clients en parlent le mieux"
    },
    {
      "type": "text",
      "id": "rating",
      "label": "Note",
      "default": "4,7"
    },
    {
      "type": "text",
      "id": "review_count",
      "label": "Nombre d'avis",
      "default": "1 067"
    }
  ],
  "presets": [
    {
      "name": "Avis Trustpilot"
    }
  ]
}
{% endschema %}